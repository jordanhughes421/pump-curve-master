# Environment
NODE_ENV=development

# Database URLs
# Development (SQLite) - Uncomment for local development
# DATABASE_URL="file:./prisma/dev.db"

# Production (PostgreSQL) - Will be set by Heroku automatically, this is just an example
# DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Auth secrets
NEXTAUTH_SECRET=your-nextauth-secret
NEXTAUTH_URL=http://localhost:3000

# For production on Heroku, these would be set to:
# NEXTAUTH_URL=https://your-app-name.herokuapp.com
# NODE_ENV=production 
